Answer-1:
var, let and const are used to declare variables in javascript.
If we use "var", we can declare a variable with the same name.It get hoisted and initialized as undefined.
If we use "let" to declare a variable ,We can change it's value any time.let is blocked scoped and it is hoisted but not initialized
If we use "const" to declara a variable, It is blocked scoped and we can't change it's value.



Answer-2:
The spread operator in js is used to spread the elements of an array.



Answer-3:
map() creates a new array by transforming every single element of an array.

filter() creates a new array with those elements which are passed by a condition.

filter() loops through an array but doesn't return a new array


